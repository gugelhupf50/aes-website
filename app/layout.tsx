import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'American Enterprise Solutions - Advanced OCR & PDF Data Extraction',
  description: 'Enterprise-grade OCR and PDF data extraction solutions. Modern replacement for Oracle WebCenter Forms Recognition.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}